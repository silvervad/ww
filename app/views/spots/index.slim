// spots # index
//=====================

- provide(:title, @country.name)

#spots-index
  
  .nav-bar Spots in #{@country.name}
  
  ul
    - @spots.each do |spot|
      li = link_to spot.name, country_spot_path(@country, spot)
    
  
  
  br

  = link_to 'New Spot', new_country_spot_path(@country)


#map-canvas 

//= simple_format @spots.to_yaml
//= @markers.inspect

