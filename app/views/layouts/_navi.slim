#navi-wrap
  ul.navbar
    li = link_to "World", root_path, id: "logo"
    - if @country.present? 
        a href="#" >
        li = link_to @country.name, country_path(@country)
        - if @spot.present? and action_name!='new'
          a href="#" >
          li = link_to @spot.name, country_spot_path